#include<stdio.h>
#include<stdlib.h>

char rotatestring(char str[], int s){
    int i;
    char temp = '\0'; 
    int length = strlen(str);
    for(i=0; i<s; i++){
        temp = str[i];
        str[i] = 
    }
}